@model List<ResultBlogDto>
@*
    Latest 3 Blogs Component
*@

<div class="sidebar-box bg-transparent">
	<h3 class="heading">Son Eklenen Bloglar</h3>
	<div class="post-entry-sidebar">
		<ul>
			@foreach (var item in Model)
			{
				<li>
					<a href="/Blog/BlogDetails/@item.Id">
						<img src="@item.CoverImage" alt="Image placeholder" class="me-4 rounded">
						<div class="text">
							<h4>@item.Title</h4>
							<div class="post-meta">
								<span class="mr-2">@item.CreatedDate.ToShortDateString() </span>
							</div>
						</div>
					</a>
				</li>
			}
		</ul>
	</div>
</div>